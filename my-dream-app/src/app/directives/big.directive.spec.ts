import { BigDirective } from './big.directive';

describe('BigDirective', () => {
  it('should create an instance', () => {
    const directive = new BigDirective();
    expect(directive).toBeTruthy();
  });
});
